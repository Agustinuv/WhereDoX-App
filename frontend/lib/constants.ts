/**
 * Mirrors app/core/constants.py. The backend is the authority — it rejects anything over
 * the limit regardless of what the UI allows — this copy only keeps the form honest.
 */
export const MAX_PROPOSED_DATES = 5;
